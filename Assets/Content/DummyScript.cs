using UnityEngine;

public class DummyScript : MonoBehaviour
{
    [SerializeField, Tooltip("Reference to the model object to create expandable foldout in search results")]
    private GameObject model;
}
